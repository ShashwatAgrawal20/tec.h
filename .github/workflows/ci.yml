name: CI

on:
  push:
    branches: [ master ]
  pull_request:

env:
  TERM: xterm-256color

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build and run tests
        run: make workflow_test
